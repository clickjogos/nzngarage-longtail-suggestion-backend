apiVersion: v1
kind: Service
metadata:
  labels:
    app: nzngarage-longtail-suggestion-backend
  name: nzngarage-longtail-suggestion-backend-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: nzngarage-longtail-suggestion-backend
  type: LoadBalancer